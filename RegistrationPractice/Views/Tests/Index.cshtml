@model IEnumerable<RegistrationPractice.Entities.Test>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LostOrFoundItem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NoReward)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reward)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LostOrFoundItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NoReward)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reward)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
